# card
A custom binary container format that bundles a file with its metadata (tags, source, description, etc.) into a single .card file. Preserves original filename and extension for seamless extraction.
